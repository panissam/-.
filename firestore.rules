rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY DEBUG RULES - allows anyone signed in to read/write everything
    // We will harden this once we confirm connectivity
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
